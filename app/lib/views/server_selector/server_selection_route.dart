import 'package:digital_signage/views/server_selector/server_selector.dart';
import 'package:flutter/material.dart';

class ServerSelectionRoute extends StatelessWidget {
  const ServerSelectionRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const ServerSelector();
  }
}
